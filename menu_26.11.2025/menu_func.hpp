#pragma once 
#include "menu.hpp"

namespace DmMog {
    const MenuItem* show_menu1(const MenuItem* current);
    const MenuItem* show_menu2(const MenuItem* current);
    const MenuItem* show_menu3(const MenuItem* current);

    const MenuItem* menu_exit(const MenuItem* current);

    const MenuItem* go_back(const MenuItem* current);

    const MenuItem* fe_vlad(const MenuItem* current);
    const MenuItem* fe_sah(const MenuItem* current);
    const MenuItem* fe_hab(const MenuItem* current);


}